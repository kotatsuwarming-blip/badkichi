# TASK-0016: /onboarding（静的）

**タスクID**: TASK-0016
**タスクタイプ**: DIRECT
**推定工数**: 3時間
**フェーズ**: Phase 3 - UI層
**信頼性レベル**: 🔵 *REQ-003 + design-interview Q2 + architecture.md §画面構成 (注)*
**状態**: ✅ 完了 (2026-06-01)

## 関連文書
- 概要: [📋 overview.md](overview.md)
- 要件定義: [📋 requirements.md](../../spec/auth-onboarding/requirements.md)
- 設計文書: [📐 architecture.md](../../design/auth-onboarding/architecture.md)
- データフロー: [🔄 dataflow.md](../../design/auth-onboarding/dataflow.md)
- エラー実装規約: [error-handling.md](../../design/cross-cutting/error-handling.md)

## タスク概要
Group 未所属ユーザ向けのオンボーディング画面を実装する (REQ-003)。MVP では**手入力フォームを提供しない** (design-interview Q2 で承認)。「Group を作る」ボタン (→ `/groups/new`) と、「発行者から受け取った招待 URL を直接開いてください」という**説明テキスト**を表示する静的画面である。招待参加は `/join/[code]` への URL 直リンク着地のみで、コード手入力系の識別子 (`INVITATION_NOT_FOUND_BY_CODE` 等) は追加しない (error-handling.md §5.2 と整合)。composable 不要の静的画面。layout は無指定で `default.vue` を継承する。

## 依存タスク
- **前提タスク**: TASK-0014 (layouts)
- **後続タスク**: TASK-0020 (E2E / 受入)

## 完了条件
- [x] 「Group を作る」ボタンを表示し押下で `/groups/new` へ遷移する (REQ-003)
- [x] 「発行者から受け取った招待 URL を直接開いてください」という説明テキストを表示する (手入力フォームなし、design-interview Q2)
- [x] コード手入力フォーム / 手入力系識別子を追加していない (design-interview Q2 / error-handling.md §5.2)
- [x] 文言は locales 経由 (NFR-204)
- [x] layout を指定せず `default.vue` を継承している (ADR-011 D1)

---

## 実装詳細
### 1. onboarding.vue (静的画面) 🔵
**信頼性**: 🔵 *REQ-003 + design-interview Q2 + architecture.md §画面構成 (注)*
2 つの動線を提示する静的画面。
- 「Group を作る」ボタン (`<UButton>`) → `/groups/new` へ navigate。
- 「招待 URL を直接開いてください」という説明テキスト (手入力フォームなし)。

composable は使わない (architecture.md §画面構成: `/onboarding` は「静的、2 ボタン」)。未所属専用のアクセス制御は middleware (REQ-102/103) が担うため page 内に認証/所属判定は書かない。layout は無指定で `default.vue` を自動適用 (認証後 page、ADR-011 D1)。文言は locales 経由 (NFR-204)。
**実装ファイル**: `app/pages/onboarding.vue`

---

## 単体テスト要件
本タスクでは単体テストなし。静的画面でロジック (composable / 条件分岐 / 入力検証) を持たないため (NFR-301: UI 見た目テストは書かない)。未所属専用アクセス制御は middleware (TASK-0013) のテストで検証済。

## 統合テスト要件
該当なし。RLS/RPC を触らない静的画面。

## UI/UX要件
### ローディング状態 N/A
**信頼性**: 🔵 *REQ-003 (静的画面)*
非同期処理を持たないため該当なし。

### エラー表示 N/A
**信頼性**: 🔵 *design-interview Q2 (手入力フォームなし)*
入力もエラー発生源も持たないため該当なし。手入力系識別子 (`INVITATION_NOT_FOUND_BY_CODE`) は追加しない (design-interview Q2 / error-handling.md §5.2)。

### モバイル対応 🔵
**信頼性**: 🔵 *Nuxt UI v4 (architecture.md §フロントエンド)*
2 択 (ボタン + 説明テキスト) を Nuxt UI v4 でモバイル幅でも崩れず提示する。

### アクセシビリティ 🟡
**信頼性**: 🟡 *Nuxt UI v4 標準 (推測)*
`<UButton>` の標準 aria に従う。

### 2 択提示 🔵
**信頼性**: 🔵 *design-interview Q2*
「Group を作る」ボタンと「招待 URL を直接開いてください」説明テキストの 2 動線のみを提示する。

---

## 実装手順
DIRECT タスクのため `direct-setup` → `direct-verify` の 2 ステップで進める。

1. **direct-setup**:
   - `app/pages/onboarding.vue` を作成。「Group を作る」ボタン (→ `/groups/new`) + 招待 URL 説明テキスト。
   - 文言は locales 経由 (NFR-204)。必要キーが無ければ追加。
   - layout は指定しない (`default.vue` 継承)。
2. **direct-verify**:
   - `pnpm typecheck` / `pnpm dev`。
   - 「Group を作る」が `/groups/new` へ遷移すること、説明テキストが表示されること、手入力フォームが無いことを目視確認。
   - `default.vue` ヘッダー (ログアウト) が継承されていることを確認。

---

## 注意事項
- 未所属専用のアクセス制御は middleware (REQ-102/103) が担当する。page 内に認証/所属判定を書かない。
- 招待参加は `/join/[code]` への URL 直リンク着地のみ (design-interview Q2)。コード手入力フォームは MVP で提供しない。
- `INVITATION_NOT_FOUND_BY_CODE` 等の手入力系識別子は追加しない (error-handling.md §5.2: MVP は `_BY_LINK` の 1 系統のみ)。

---

## 信頼性レベルサマリー
| カテゴリ | 🔵 | 🟡 | 🔴 | 合計 |
|---|---|---|---|---|
| 実装詳細 | 1 | 0 | 0 | 1 |
| UI/UX要件 | 4 | 1 | 0 | 5 |
- **総項目数**: 6 / 🔵 5(83%) / 🟡 1(17%) / 🔴 0(0%)
**品質評価**: 高品質
