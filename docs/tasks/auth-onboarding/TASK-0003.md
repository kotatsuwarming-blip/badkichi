# TASK-0003: エラー識別子定数 error-codes.ts

**タスクID**: TASK-0003
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *error-handling.md §4.1 + interfaces.ts §1 + REQ-105 / REQ-407*

## 関連文書
- 概要: [📋 overview.md](overview.md)
- 要件定義: [📋 requirements.md](../../spec/auth-onboarding/requirements.md)
- 設計文書: [📐 architecture.md](../../design/auth-onboarding/architecture.md)
- エラー実装規約: [error-handling.md](../../design/cross-cutting/error-handling.md)

## タスク概要
`app/types/error-codes.ts` を error-handling.md §4.1 に従って作成する。`APP_ERROR_CODES` には error-handling.md §4.1 の 6 識別子に加えて、本単位で `ALREADY_IN_GROUP: 'already_in_group'` を追加する (REQ-105、DB 例外 `already_in_group` と 1:1、interfaces.ts §1 `APP_ERROR_CODES_ADDITION` で確定)。PG SQLSTATE 定数・型・型ガード (`isAppError` / `isPgError`) も合わせて定義し、生文字列比較を禁止する基盤 (REQ-407) を整える。

## 依存タスク
- **前提タスク**: なし
- **後続タスク**: TASK-0007 (useErrorMessage が本定数を消費)

## 完了条件
- [ ] `APP_ERROR_CODES` に NOT_AUTHENTICATED / NOT_A_MEMBER / INVALID_GROUP_NAME / INVITATION_NOT_FOUND_BY_LINK / INVITATION_EXPIRED / INVITATION_CODE_COLLISION_AFTER_RETRY + **ALREADY_IN_GROUP** が定義されている
- [ ] `PG_ERROR_CODES` (UNIQUE_VIOLATION / FK_VIOLATION / CHECK_VIOLATION / RLS_REJECTED) と `AppErrorCode` / `PgErrorCode` / `ErrorContext` 型が定義されている
- [ ] `isAppError` / `isPgError` 型ガードが定義されている
- [ ] `pnpm typecheck` が pass する

---

## 実装詳細
### 1. エラー識別子定数 🔵
**信頼性**: 🔵 *error-handling.md §4.1 + interfaces.ts §1 APP_ERROR_CODES_ADDITION + REQ-105 / REQ-407*
error-handling.md §4.1 のコードをベースに、`APP_ERROR_CODES` の末尾に `ALREADY_IN_GROUP: 'already_in_group'` を 1 行追記する。`ALREADY_IN_GROUP` は error-handling.md §4.1 には未定義で、本単位の追加対象 (architecture.md §既存 API マッピング 注3、REQ-105)。`ErrorContext` 型は `'join_group' | 'create_group' | 'generic'` (interfaces.ts §4)。
**実装ファイル**: `app/types/error-codes.ts`
```ts
// App 側ドメインエラー (DB 側 RAISE EXCEPTION のメッセージと 1:1)
export const APP_ERROR_CODES = {
  NOT_AUTHENTICATED: 'not_authenticated',
  NOT_A_MEMBER: 'not_a_member',
  INVALID_GROUP_NAME: 'invalid_group_name',
  // MVP: URL 直リンク着地のみ。将来手入力フォームを追加する場合は _BY_CODE を別識別子として定義
  INVITATION_NOT_FOUND_BY_LINK: 'invitation_not_found_by_link',
  INVITATION_EXPIRED: 'invitation_expired',
  INVITATION_CODE_COLLISION_AFTER_RETRY: 'invitation_code_collision_after_retry',
  // auth-onboarding 追加 (REQ-105 / DB 例外 already_in_group と 1:1、interfaces.ts §1)
  ALREADY_IN_GROUP: 'already_in_group'
} as const

export type AppErrorCode = typeof APP_ERROR_CODES[keyof typeof APP_ERROR_CODES]

// PostgreSQL SQLSTATE (PG 標準コード、変わらない)
export const PG_ERROR_CODES = {
  UNIQUE_VIOLATION: '23505',
  FK_VIOLATION: '23503',
  CHECK_VIOLATION: '23514',
  RLS_REJECTED: '42501'
} as const

export type PgErrorCode = typeof PG_ERROR_CODES[keyof typeof PG_ERROR_CODES]

// PG エラーで context による出し分けが必要な場合に使う
// App 識別子は 1:1 マッピングのため context を使わない
export type ErrorContext = 'join_group' | 'create_group' | 'generic'

export function isAppError(error: unknown, code: AppErrorCode): boolean {
  return typeof error === 'object'
    && error !== null
    && 'message' in error
    && typeof (error as { message: unknown }).message === 'string'
    && (error as { message: string }).message.includes(code)
}

export function isPgError(error: unknown, code: PgErrorCode): boolean {
  return typeof error === 'object'
    && error !== null
    && 'code' in error
    && (error as { code: unknown }).code === code
}
```

---

## 単体テスト要件
ADR-012 §影響表に error-codes 専用テストは含まれない。`isAppError` / `isPgError` は `useErrorMessage` 経由 (TASK-0007) で検証されるため、本タスクでは単体テストを省略する (memory `feedback_test_coverage` の最小カバレッジ方針: 同じ分岐を二重に検証しない)。本タスクは型定数の宣言のみで分岐ロジックを持たず、`pnpm typecheck` で型整合性を確認する。

## 統合テスト要件
該当なし。本単位は新規 RLS/RPC を作らない。

## UI/UX要件
N/A (型定義、UI 変更を含まない)。

---

## 実装手順
### DIRECTタスクの場合
1. `/tsumiki:direct-setup`
2. `/tsumiki:direct-verify`

---

## 注意事項
- 生文字列比較禁止 (REQ-407)。page / composable で `'invitation_expired'` 等のリテラルを直接比較しない。
- `INVITATION_NOT_FOUND_BY_LINK` の値 `'invitation_not_found_by_link'` は DB の `'invitation_not_found'` と**文字列が異なる**ため、`useJoinGroup` (TASK-0011) 側で DB メッセージから App 識別子への明示変換が必要 (素朴な `includes` に頼らない、architecture.md §既存 API マッピング 注2)。

---

## 信頼性レベルサマリー
| カテゴリ | 🔵 | 🟡 | 🔴 | 合計 |
|---|---|---|---|---|
| 実装詳細 | 1 | 0 | 0 | 1 |
| 単体テスト | 0 | 0 | 0 | 0 |
| UI/UX要件 | 0 | 0 | 0 | 0 |
- **総項目数**: 1
- 🔵 1(100%) / 🟡 0(0%) / 🔴 0(0%)
**品質評価**: 高品質
