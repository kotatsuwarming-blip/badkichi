# TASK-0002: determineSetWinner 実装

**TaskID**: TASK-0002
**タイプ**: TDD
**見積もり**: 3時間
**フェーズ**: Phase 1
**信頼性**: 🔵（REQ-007, REQ-101〜103, BWFルール準拠）
**作成日**: 2026-04-14

**関連文書**:
- インターフェース定義: [interfaces.ts](../../design/rule-engine/interfaces.ts)
- 要件定義書: [requirements.md](../../spec/rule-engine/requirements.md)
- 受け入れ基準: [acceptance-criteria.md](../../spec/rule-engine/acceptance-criteria.md)

---

## タスク概要

セットの勝者を判定する `determineSetWinner` 関数を実装する。Score と SetConfig のみを入力とする独立した関数。BWFバドミントンルールに基づくデュース処理（`enableDeuce` + `deucePointCap`）を正しくハンドリングする。

**配置先**: `app/utils/rule-engine/determine-set-winner.ts`
**テスト**: `app/utils/rule-engine/__tests__/determine-set-winner.test.ts`

---

## 依存タスク

### 前提タスク
- **TASK-0001**: 型定義ファイル作成（Score, SetConfig 型が必要）

### 後続タスク
- TASK-0006: index.ts 公開API

---

## 完了条件

- [ ] `determineSetWinner` 関数が実装されている
- [ ] デュース有効時（`enableDeuce: true`）が正しく動作する
- [ ] デュース無効時（`enableDeuce: false`）が正しく動作する
- [ ] 全テストケースが通る
- [ ] `pnpm typecheck` でエラーなし

---

## 関数仕様

```typescript
export function determineSetWinner(score: Score, config: SetConfig): Team | null
```

### ロジック: デュース有効時（enableDeuce: true）

```
cap = config.deucePointCap

// キャップによるサドンデス勝利
if (scoreA === cap) return 'A'
if (scoreB === cap) return 'B'

// 通常勝利（targetPoints 以上 & 2点差以上）
if (scoreA >= targetPoints && scoreA - scoreB >= 2) return 'A'
if (scoreB >= targetPoints && scoreB - scoreA >= 2) return 'B'

return null
```

### ロジック: デュース無効時（enableDeuce: false）

```
if (scoreA >= targetPoints) return 'A'
if (scoreB >= targetPoints) return 'B'
return null
```

---

## テストケース

| テストID | テスト名 | 入力 (scoreA, scoreB, target, enableDeuce, cap) | 期待結果 |
|----------|---------|------------------------------------------------|---------|
| TC-01 | 通常勝利 21-18 | 21, 18, 21, true, 30 | `'A'` |
| TC-02 | デュース中 22-21 | 22, 21, 21, true, 30 | `null` |
| TC-03 | デュース2点差勝利 24-22 | 24, 22, 21, true, 30 | `'A'` |
| TC-04 | デュースなし 21-20 | 21, 20, 21, false, 21 | `'A'` |
| TC-05 | 目標15点 12-15 | 12, 15, 15, true, 24 | `'B'` |
| TC-06 | サドンデス 30-29 | 30, 29, 21, true, 30 | `'A'` |
| TC-07 | セット未完了 15-12 | 15, 12, 21, true, 30 | `null` |
| TC-08 | 0-0 | 0, 0, 21, true, 30 | `null` |

---

## 実装手順（TDD）

1. 失敗するテストを作成
2. テストが通る最小実装
3. リファクタリング（必要な場合のみ）
