# TASK-0001: 型定義ファイル作成 (types.ts)

**TaskID**: TASK-0001
**タイプ**: DIRECT
**見積もり**: 1時間
**フェーズ**: Phase 1
**信頼性**: 🔵
**作成日**: 2026-04-10
**更新日**: 2026-04-14（増分計算アーキテクチャに対応）
**状態**: ✅ 完了

**関連文書**:
- インターフェース定義: [interfaces.ts](../../design/rule-engine/interfaces.ts)

---

## タスク概要

`docs/design/rule-engine/interfaces.ts` に定義された型を基に、`app/utils/rule-engine/types.ts` を作成する。

**配置先**: `app/utils/rule-engine/types.ts`

---

## 定義済みの型

| 型名 | 種別 | 説明 |
|------|------|------|
| `Team` | type alias | `'A' \| 'B'` |
| `CourtSide` | type alias | `'left' \| 'right'` |
| `PlayerId` | type alias | `string` |
| `SetConfig` | interface | セットの設定 |
| `SetPlayerPosition` | interface | 選手の初期位置 |
| `RallyResult` | interface | ラリー結果（applyRally への入力） |
| `Score` | interface | スコア |
| `TeamPositions` | interface | 4人の左右位置 |
| `GameState` | interface | ゲームの現在の状態 |
| `SetResult` | interface | セット結果（勝者 + スコア） |
