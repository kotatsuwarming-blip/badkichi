# TASK-0006: index.ts 公開API

**TaskID**: TASK-0006
**タイプ**: DIRECT
**見積もり**: 1時間
**フェーズ**: Phase 1
**信頼性**: 🔵
**作成日**: 2026-04-14

**関連文書**:
- アーキテクチャ設計: [architecture.md](../../design/rule-engine/architecture.md)

---

## タスク概要

rule-engine の全公開関数と型を `index.ts` から re-export する。Nuxt の auto-import で `app/utils/rule-engine/` から直接使えるようにする。

**配置先**: `app/utils/rule-engine/index.ts`

---

## 依存タスク

### 前提タスク
- **TASK-0002**: determineSetWinner
- **TASK-0003**: createInitialState
- **TASK-0004**: applyRally
- **TASK-0005**: applyOverride

---

## 完了条件

- [ ] `index.ts` が作成され、全関数・全型が re-export されている
- [ ] `pnpm typecheck` でエラーなし
- [ ] `pnpm lint` でエラーなし

---

## 実装内容

```typescript
// 関数
export { createInitialState } from './create-initial-state'
export { applyRally } from './apply-rally'
export { applyOverride } from './apply-override'
export { determineSetWinner } from './determine-set-winner'

// 型
export type {
  Team,
  CourtSide,
  PlayerId,
  SetConfig,
  SetPlayerPosition,
  RallyResult,
  Score,
  TeamPositions,
  GameState,
  SetResult
} from './types'
```
