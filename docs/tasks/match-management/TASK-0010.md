# TASK-0010: 受入検証 統合確認

**タスクID**: TASK-0010
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 2 - UI 層
**信頼性レベル**: 🔵 *acceptance-criteria.md 出典（全 TC 🔵94% / 🟡6%）*

## 関連文書
- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/match-management/requirements.md) - 全 REQ / 全 EDGE / 全 NFR
- **受け入れ基準**: [✅ acceptance-criteria.md](../../spec/match-management/acceptance-criteria.md)
- **設計文書**: [📐 architecture.md](../../design/match-management/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/match-management/dataflow.md)

## タスク概要

match-management ユニットの **受入検証タスク**。Phase 1〜2 の全実装（migration / 型 / `match-form.ts` /
composable / `MatchFormModal` / `matches.vue` / i18n）が出揃った状態で、`acceptance-criteria.md` の全
テストケース（TC-001〜004 + 状態 / 非機能 / Edge）が満たされることを統合的に確認し、未充足を洗い出す。

確認対象 TC: TC-001（01/02/E01）、TC-002（01/02/03/E01〜E06/B01/B02）、TC-003（01/02/E01/E02）、
TC-004（01/02/E01）、TC-005（01/E01）、状態（TC-201-01/TC-203-01/TC-202-01）、
非機能（TC-NFR-101-01/TC-NFR-302-01/TC-NFR-301-01）、Edge（TC-EDGE-004-01/007-01/009-01/010-01）。
あわせて `pnpm lint` / `pnpm typecheck` / 既存ユニットテスト緑、i18n キー構造一致 CI 緑、
一覧 → 追加 → 編集 → 削除（確認承認）の通しフロー確認、local/youtube 両系の作成、選手 4 人未満、
削除済 player 名維持（EDGE-007）の手動検証観点を整理する。

新規実装は原則持たず、**未充足発見時のみ**該当タスクへ差し戻す。新規結合テストは最小限
（フロー結合 happy path 1 本程度）に留める（NFR-303 / feedback_test_coverage）。

## 依存タスク
- **前提タスク**: [TASK-0009](TASK-0009.md)（Phase 2 全実装完了）※間接的に TASK-0001〜0008 すべて
- **後続タスク**: なし

## 完了条件
- [ ] acceptance-criteria.md の全 TC を実装にマッピングし、充足を確認した
- [ ] 未充足 TC があれば該当タスクへ差し戻し、解消した
- [ ] `pnpm lint` が通る
- [ ] `pnpm typecheck` が通る
- [ ] 既存ユニットテスト（schema / composable / component / page）が全緑
- [ ] i18n キー構造一致 CI（ja / en）が緑（NFR-301）
- [ ] 一覧 → 追加 → 編集 → 削除（確認承認）の通しフロー結合テスト（happy path 1 本）が緑
- [ ] NFR-101（RLS 他 Group 不可）/ NFR-001（部分インデックス・`deleted_at IS NULL`）を構造的に確認した
- [ ] 手動検証観点（local/youtube 作成・選手 4 人未満・削除済 player 名維持）を整理した

---

## 検証詳細（受入 TC マッピング）

### 1. 機能要件 TC のマッピング 🔵
**信頼性**: 🔵 *acceptance-criteria.md*

| TC | 内容 | 充足箇所 | 検証手段 |
|----|------|---------|---------|
| TC-001-01 | 未削除試合 N 件 → N 行・新しい順 | `useMatches`（order 降順）/ `matches.vue` 一覧 | composable 単体（TASK-0003）+ page 単体（TASK-0009 TC1/2） |
| TC-001-02 | 削除済（deleted_at）は非表示 | `useMatches` の `.is('deleted_at', null)` | composable 単体 + クエリ構造確認 |
| TC-001-E01 | 他 Group は RLS で取得不可 | matches_select RLS / group_id 経由 | 構造確認（data-foundation integration が担保） |
| TC-002-01 | 試合名+日付+4 選手+local → 作成・file.name ラベル | `MatchFormModal` + `useCreateMatch` | component 単体（TASK-0008 TC6/7）+ フロー結合 |
| TC-002-02 | 試合名+日付+4 選手+youtube → 作成・URL 保存 | 同上 | component 単体（TASK-0008 TC7）+ フロー結合 |
| TC-002-03 | 試合名未入力 → 作成・対戦カード表示 | `match-form.ts` 任意 + `matches.vue` cardLabel | schema 単体（TASK-0004）+ page 単体（TASK-0009 TC2） |
| TC-002-E01 | 4 選手重複 → 拒否 | `matchFormSchema` refine + 選択肢除外 | schema 単体 + component 単体（TASK-0008 TC4/5） |
| TC-002-E02 | 選手スロット未選択 → 拒否 | `matchFormSchema`（uuid 必須） | schema 単体（TASK-0004） |
| TC-002-E03 | 動画ソース未指定 → 拒否 | `matchFormSchema`（videoSourceUrl 非空） | schema 単体 + component 単体（TASK-0008） |
| TC-002-E04 | 他 Group player 指定 → 選択肢に出ず複合 FK で拒否 | `usePlayers` 自 Group 限定 + 複合 FK | 構造確認（data-foundation） |
| TC-002-E05 | 試合日付未入力 → 拒否 | `matchFormSchema`（match_date 必須） | schema 単体 + component 単体（TASK-0008 TC2） |
| TC-002-E06 | 試合名 51 字 → 拒否 / 50 字 → 許可 | `matchFormSchema`（1〜50 字） | schema 単体 + component 単体（TASK-0008 TC1） |
| TC-002-B01 | 同一カードを 2 件作成 → 成功・2 行 | matches に UNIQUE 無し | composable 単体（TASK-0005）+ フロー結合 |
| TC-002-B02 | 同一 match_date 複数 → created_at 降順 | `useMatches` order 二段 | composable 単体（TASK-0003）+ クエリ構造確認 |
| TC-003-01 | 選手 1 名差し替え → 更新 | `MatchFormModal` edit + `useUpdateMatch` | component 単体（TASK-0008 TC9）+ フロー結合 |
| TC-003-02 | 動画ソース local→youtube 変更 → 更新 | `MatchFormModal` 切替 + `useUpdateMatch` | component 単体（TASK-0008 TC6/9） |
| TC-003-E01 | 更新後 4 選手重複 → 拒否 | `matchFormSchema` refine（mode 非依存） | schema 単体 + component 単体 |
| TC-003-E02 | スロット空で更新 → 拒否 | `matchFormSchema`（uuid 必須） | schema 単体 |
| TC-004-01 | 削除 → 確認 → 承認 → 一覧消失 | `matches.vue` 確認ダイアログ + `useDeleteMatch` | page 単体（TASK-0009 TC5）+ composable 単体（TASK-0006） |
| TC-004-02 | 確認でキャンセル → 削除されない | `matches.vue` キャンセル分岐 | page 単体（TASK-0009 TC6） |
| TC-004-E01 | 物理 DELETE なし（deleted_at UPDATE のみ） | `useDeleteMatch`（update 実装） | composable 単体 + コードレビュー（`.delete()` 不在） |
| TC-005-01 | 同一 Group の任意メンバーが操作可 | RLS is_member_of（ロール列なし） | 構造確認（data-foundation） |
| TC-005-E01 | 非メンバーは RLS で拒否 | matches_* RLS | 構造確認（data-foundation） |

### 2. 状態要件 TC のマッピング 🔵/🟡
**信頼性**: 🔵 *REQ-201 / REQ-202* / 🟡 *REQ-203*

| TC | 内容 | 充足箇所 | 検証手段 |
|----|------|---------|---------|
| TC-201-01 | 試合 0 件 → 空状態 + CTA | `matches.vue` 空状態分岐 | page 単体（TASK-0009 TC3） |
| TC-203-01 🟡 | 選手 < 4 人 → 作成不可 + 導線 | `matches.vue` 選手不足分岐 | page 単体（TASK-0009 TC4） |
| TC-202-01 | Group 未所属 → /onboarding 誘導 | `auth.global.ts`（既存） | auth-onboarding integration が担保（構造確認） |

### 3. 非機能要件 TC の構造的確認 🔵
**信頼性**: 🔵 *NFR-101 / NFR-001 / NFR-301 / NFR-302 / database-schema.sql*

| TC | 内容 | 検証手段 |
|----|------|---------|
| TC-NFR-101-01 | 他 Group の select/insert/update 不可 | matches RLS（is_member_of）の構造確認。実 RLS は data-foundation の integration test が担保済みのため、本ユニットでは「全クエリが group_id 経由・直叩きなし」を構造確認 |
| TC-NFR-302-01 | page/component が composable 経由・supabase 直叩きなし | `matches.vue` / `MatchFormModal` を grep し supabase クライアント直呼びが無いことを点検（REQ-403） |
| TC-NFR-301-01 | 全文言 ja/en 存在・キー構造一致 | i18n キー構造一致 CI（`matches` namespace + `errors.*`）緑 |
| TC-NFR-001（NFR-001）| 一覧クエリが `deleted_at IS NULL` を含み部分インデックス対象 | `useMatches` クエリに `.is('deleted_at', null)` が含まれることをコード / composable 単体で確認 |

### 4. Edge ケース TC のマッピング 🔵/🟡
**信頼性**: 🔵 *EDGE-007 / 009 / 010* / 🟡 *EDGE-004*

| TC | 内容 | 充足箇所 | 検証手段 |
|----|------|---------|---------|
| TC-EDGE-004-01 🟡 | youtube に URL でない文字列 → 最低限「空でない」保証 | `matchFormSchema`（youtube refine） | schema 単体 + component 単体（TASK-0008 TC3） |
| TC-EDGE-007-01 | 試合参照中 player が削除後も一覧で名前維持 | `useMatches` 埋め込み解決（未削除フィルタなしの名前解決 / フォールバック） | composable 単体（TASK-0003）+ 手動検証 |
| TC-EDGE-009-01 | 入力検証エラーは UFormField inline | `MatchFormModal` inline 表示 | component 単体（TASK-0008 TC1〜4） |
| TC-EDGE-010-01 | RLS 拒否・通信エラーは useToast 通知 | `MatchFormModal` / `matches.vue` toast | component / page 単体（TASK-0008 TC8 / TASK-0009 TC5 失敗系） |

### 5. 横断チェック 🔵
**信頼性**: 🔵 *CLAUDE.md commands / NFR-301*

- `pnpm lint`：ESLint（1tbs / no comma dangle）緑
- `pnpm typecheck`：型エラーなし（`MatchListItem` / 入力型 / composable 契約整合、再生成済 supabase 型）
- 既存ユニットテスト全緑（`tests/unit/**`）
- i18n キー構造一致 CI：ja / en の `matches` namespace + `errors.*`（検証エラー）が一致（NFR-301）

---

## 単体テスト要件

> 本タスクは既存単体テスト（TASK-0001〜0009）の**集約確認**が主。新規単体テストは原則追加しない。
> 不足が見つかった TC のみ、該当タスクのテストファイルに最小ケースを追補する（NFR-303 / feedback_test_coverage）。

### 検証ケース1: 全 TC の充足マッピング完了 🔵
**信頼性**: 🔵 *acceptance-criteria.md*
**Given**: Phase 1〜2 実装完了 / **When**: 上表マッピングを点検 / **Then**: 全 TC が充足箇所を持ち、
未充足が 0 件（あれば該当タスクへ差し戻し）

### 検証ケース2: コマンド緑（lint / typecheck / unit） 🔵
**信頼性**: 🔵 *CLAUDE.md commands*
**Given**: 全実装 / **When**: `pnpm lint && pnpm typecheck && pnpm test`（unit）/ **Then**: いずれも成功終了

---

## 統合テスト要件

### 統合テスト1: 一覧 → 追加 → 編集 → 削除（確認承認）通しフロー（happy path 1 本） 🔵
**信頼性**: 🔵 *acceptance-criteria.md TC-002/003/004 / dataflow.md 機能1〜4*
**テストファイル**: `tests/unit/pages/groups/matches.flow.test.ts`（結合スタブ・happy-dom）
**テスト内容**: `matches.vue` をマウントし、composable は最小限の結合スタブ（in-memory 配列で
create / update / delete / list を連動、`usePlayers` は 4 人以上）に差し替えたうえで、
1. 初期 0 件 → 空状態
2. 追加 CTA → モーダルで試合名/日付/4 選手/youtube 入力 → 保存 → 一覧 1 行
3. 編集 → 試合名変更 → 保存 → 行の値更新
4. 削除 → 確認ダイアログ承認 → 行消失
を 1 シナリオで検証する。
**期待結果**: 各ステップで一覧 DOM が期待どおり更新される（refresh 連動）。冗長な異常系は別タスクの
単体テストに委ねるため、ここでは happy path のみ。

```typescript
// @vitest-environment happy-dom
// in-memory 結合スタブで useMatches/useCreateMatch/useUpdateMatch/useDeleteMatch/usePlayers を連動させ、
// 追加→編集→削除（確認承認）で一覧 DOM が更新されることを 1 本で確認する（happy path）。
```

### 統合テスト2: RLS / 構造的保証の確認（非実行・点検） 🔵
**信頼性**: 🔵 *NFR-101 / NFR-001 / data-foundation integration test 既存*
**テスト内容**: NFR-101 の実 RLS 越境拒否・複合 FK 拒否・distinct CHECK は data-foundation の
integration test（matches RLS / FK / CHECK）が既に担保済み。本ユニットでは新規 integration test を
**追加せず**、(a) 全クエリが composable 経由で group_id を伴うこと、(b) `useMatches` が
`deleted_at IS NULL` を含むこと、(c) 削除が `deleted_at` UPDATE で物理 `.delete()` を呼ばないこと、を
コードレビューで点検する。
**期待結果**: 越境クエリ経路が存在せず、一覧クエリが未削除フィルタを含み、物理削除が混入しない。

---

## 手動検証観点（受入）

> 自動テストで担保しきれない実ブラウザ挙動を手動確認する。新規実装は持たない。

### 観点1: 作成 → 一覧 → 編集 → 削除 の通し（実 Supabase / dev） 🔵
**信頼性**: 🔵 *TC-002/003/004*
試合を 1 件作成 → 一覧に新しい順で出る → 編集で試合名・選手を変更 → 反映 → 削除（確認承認）で消える。

### 観点2: local / youtube 両系の作成 🔵
**信頼性**: 🔵 *TC-002-01 / TC-002-02 / REQ-106 / REQ-107*
- local: ファイル選択 → `video_source_url` に元ファイル名が保存され、一覧/編集で確認できる。
- youtube: URL 入力 → 動画 ID 抽出後の値が保存される。URL でない文字列は inline error（EDGE-004）。

### 観点3: 選手 4 人未満時の挙動 🟡
**信頼性**: 🟡 *TC-203-01 / REQ-203*
選手を 3 人以下にした Group で「試合を追加」が disabled になり、選手追加導線が出ることを確認。

### 観点4: 削除済 player の名前維持（EDGE-007） 🔵
**信頼性**: 🔵 *TC-EDGE-007-01 / EDGE-007*
試合に使われている player を player-management でソフト削除しても、試合一覧で当該選手名が引き続き
表示される（対戦カード表示が壊れない）ことを確認。

---

## UI/UX要件

### ローディング状態 🔵
一覧取得中の USkeleton 表示（TASK-0009）が機能していることを通しフローで確認する。

### エラー表示 🔵
入力検証 inline（TASK-0008）/ 通信・RLS toast（TASK-0008・0009）の 2 チャネル分離が
意図どおり機能することを点検する（受入観点での横断確認）。

### モバイル対応 🔵
一覧 / 追加編集モーダル / 削除確認ダイアログが小画面で破綻しないことを手動確認（NFR-201）。新規実装は持たない。

### アクセシビリティ 🔵
編集 / 削除ボタンの `aria-label`、モーダル / 確認ダイアログのフォーカストラップ・label 紐付け
（TASK-0008・0009）が適用済みであることを点検する。

---

## 実装手順

### TDDタスクの場合
1. `/tsumiki:tdd-requirements TASK-0010` - 受入 TC のマッピング整理
2. `/tsumiki:tdd-testcases` - 通しフロー結合テストのケース化
3. `/tsumiki:tdd-red` - フロー結合テスト実装（失敗）
4. `/tsumiki:tdd-green` - 未充足 TC があれば該当タスクで解消 → 緑化
5. `/tsumiki:tdd-refactor` - 重複テスト / スタブ整理
6. `/tsumiki:tdd-verify-complete` - 全 TC 充足・lint / typecheck / CI 緑を最終確認

---

## 注意事項
- 本タスクは**新規プロダクションコードを足さない**のが原則。未充足が見つかったら該当タスク
  （TASK-0001〜0009）へ差し戻して直す（受入タスクで実装を肥大化させない）。
- NFR-101 の実 RLS テスト・複合 FK・distinct CHECK は data-foundation 既存 integration が担保。
  **ここで RLS integration を重複作成しない**（feedback_test_coverage / 重複回避）。構造的確認に留める。
- 結合テストは happy path 1 本のみ。異常系・境界は TASK-0004 / 0008 / 0009 の単体テストが網羅済み。
- i18n キー一致は CI に委ねる。手動で目視差分点検もするが、判定は CI を真とする（NFR-301）。
- 物理削除が混入していないこと（`.delete()` 直呼びがないこと）を点検（REQ-402 / ソフト削除のみ）。
- migration（name / match_date 列追加）が適用済み・supabase 型再生成済みであることを前提に typecheck を実施
  （REQ-408 / migration 適用は CI 経由・ローカル不可）。

---

## 受入結果（2026-06-05）

✅ **全 TC 充足・未充足 0 件**。横断チェックすべて green:
- `pnpm test`（unit）: **180 passed / 45 files**（schema / composable / component / page / flow）
- `pnpm typecheck`: OK（再生成済 supabase 型と整合）
- `pnpm lint`: OK / `actionlint`: OK
- `pnpm i18n:check`: OK（ja/en キー構造一致、12 top-level、`matches` namespace + `errors.*` 検証キー）
- 通しフロー結合テスト `tests/unit/pages/groups/matches.flow.test.ts`: 空状態 → 追加 → 編集 → 削除（確認承認）の happy path 1 本 green

**構造的確認**:
- supabase 直叩きなし（`matches.vue` / `MatchFormModal.vue` は composable 経由のみ、REQ-403）
- `useMatches` は `.is('deleted_at', null)` を含む（NFR-001 部分インデックス対象）
- 物理 `.delete()` 不在（`useDeleteMatch` は `deleted_at` UPDATE のみ、REQ-402）
- NFR-101 の実 RLS 越境は data-foundation 既存 integration が担保。加えて本ユニットで
  `useMatches.integration.test.ts`（複合FK埋め込み陽性 + 越境空集合）を追加（CI 専用）

**受入で発見・解消したギャップ**:
- default layout に試合一覧への導線が無かったため、`matchesLink`（`/groups/[id]/matches`）と
  i18n `layout.default.matches` を追加（機能到達性の確保）。

**手動検証観点（実ブラウザ / dev、ユーザ確認推奨）**: ①作成→一覧→編集→削除の通し ②local（ファイル名ラベル）/
youtube（ID 抽出）両系 ③選手 4 人未満で追加 disabled + 導線 ④削除済 player の名前維持（EDGE-007）。

## 信頼性レベルサマリー
### 項目別信頼性
| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細（検証マッピング） | 4 | 1 | 0 | 5 |
| 単体テスト（検証ケース） | 2 | 0 | 0 | 2 |
| 統合テスト | 2 | 0 | 0 | 2 |
| 手動検証観点 | 3 | 1 | 0 | 4 |
| UI/UX要件 | 4 | 0 | 0 | 4 |
### 全体評価
- **総項目数**: 17項目
- 🔵 **青信号**: 15項目 (88%)
- 🟡 **黄信号**: 2項目 (12%) ※選手 4 人未満導線（REQ-203）と youtube 形式検証の程度（EDGE-004）は kairo-design 由来の妥当推測
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
